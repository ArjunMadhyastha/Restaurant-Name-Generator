# Restaurant-Name-Generator
It generates a fancy restaurant name and the menu for it based on the user's choice of cuisine
# Technologies used
-> LLM
-> Langchain
# Instructions to run
-> Have required dependencies installed
-> Go to terminal of the current file directory and run using the command 'streamlit run main.py'
-> Choose a cuisine from the sidebar and get a fancy restaurant name and menu for it.
